name: Server CI

on:
  pull_request:
    branches:
      - develop
      - main
    paths:
      - "AGRO_SERVER/**"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: AGRO_SERVER

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: AGRO_SERVER/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: echo "No linting required for server"

  security:
    name: Security audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: AGRO_SERVER

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: AGRO_SERVER/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit --audit-level=high
